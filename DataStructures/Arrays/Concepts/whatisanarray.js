//array is a linear Data Structure that stores elements in a contiguous block of memory.

//Arrays are dynamic and can store elements of different data types.
let arr = [1, "hello", true, { key: "value" }, [1, 2]];

console.log(arr);

//below would display each items inside an array
arr.forEach((item) => console.log(item));
